import React from 'react';
import { CircularProgress } from '@mui/material';

//render a loading circle during async operations
export default function Load() {
  return (
    <CircularProgress />
  );
};